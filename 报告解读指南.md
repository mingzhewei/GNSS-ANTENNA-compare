# GNSS 双天线抗干扰对比报告解读指南

本指南面向没有读过分析代码的工程师，目的是让你能独立读懂 `reports/` 下 4 份双天线对比报告
（`antenna_X_vs_Y_{测试名}_report.html`），知道每个数字是什么、怎么算出来的、怎么判断好坏。

文中所有举例数字均取自真实报告 **0728-076-560**（北天BT-T076 vs 北天BT-T560），
并交叉引用了另外 3 份报告（0728-076-617、0731-076-by403、0731-076-speed）的关键数据。

> 一句话总览：**每份报告回答两个问题——① 谁的绝对接收能力强（横向对比，两根天线同一时刻比）；
> ② 谁的抗干扰能力强（纵向对比，每根天线干扰段和自己的无干扰基线比）。两个问题必须分开看。**

---

## 一、测试是什么

### 1.1 测试布置

- **接收机**：两台北云 M21（UG016/OEM7 协议），各自接一根天线，同步记录。
- **天线**：两根待测天线相距 **30 cm** 固定于同一平面，保证看到的天空基本一致。
- **干扰源**：距天线 **15 cm**（近场），依次为对讲机 409 MHz、对讲机 392 MHz、手机（4G/5G）、Wi-Fi 路由器（2.4 GHz）。
- **数据**：每台 M21 同时输出两路——
  - **COM3 ASCII**：TRACKSTAT（每通道 C/N0、locktime、跟踪状态、reject 标志，约 5 Hz）、BESTPOS（定位状态、参与解算星数）、GPGST（伪距 RMS）、GPGSV（可见星数）；
  - **COM4 二进制**：RANGECMP（消息 ID 140，含 StdDev-PSR 伪距标准差、StdDev-ADR 载波相位标准差、locktime、C/N0，可按频点拆分）。

### 1.2 9 个片段的时序

每组测试连续采集约 7 分钟，按固定顺序切为 9 段，干扰段与无干扰段交替，
**每个干扰段的"基线"就是它紧前面的那个无干扰段**：

| 序号 | 片段 | 角色 |
|---|---|---|
| 1 | 无干扰 #1 | 基线（供 409MHz 对比） |
| 2 | **409MHz 干扰** | 干扰段（对讲机，3 次谐波 1227 MHz 贴近 L2） |
| 3 | 无干扰 #2 | 基线（供 392MHz 对比） |
| 4 | **392MHz 干扰** | 干扰段（对讲机，3 次谐波 1176 MHz 贴近 L5/E5a） |
| 5 | 无干扰 #3 | 基线（供 4G/5G 对比） |
| 6 | **4G/5G 干扰** | 干扰段（手机） |
| 7 | 无干扰 #4 | 基线（供 Wi-Fi 对比） |
| 8 | **Wi-Fi 干扰** | 干扰段（路由器 2.4 GHz） |
| 9 | 无干扰 #5 | 收尾基线 |

**分析窗口**：报告中所有"某片段"的统计数字，都不是整段平均，而是取该段**正中间 10 秒**
（稳态窗口，避开干扰开/关的过渡沿）。窗口的起止秒数在报告第 1 章逐段列出，可复核。
片段时长由测试记录人事先提供（`config/tests.yaml`），若某段被文件实际长度截断，窗口时长会带 `*` 号标注。

### 1.3 4 组测试各回答什么问题

| 测试目录 | 对比双方 | 报告文件 |
|---|---|---|
| `reports/0728-076-560` | 北天BT-T076 vs 北天BT-T560 | `antenna_BT-T076_vs_BT-T560_0728-076-560_report.html` |
| `reports/0728-076-617` | 北天BT-T076 vs 北天BT-T617 | `antenna_BT-T076_vs_BT-T617_0728-076-617_report.html` |
| `reports/0731-076-by403` | 北天BT-T076 vs by403 | `antenna_BT-T076_vs_by403_0731-076-by403_report.html` |
| `reports/0731-076-speed` | 北天BT-T076 vs speed | `antenna_BT-T076_vs_speed_0731-076-speed_report.html` |

每组测试的结构完全一样：**076 是固定基准（锚），另一根是被评估的竞品**。
每组回答的问题是："在这 4 种干扰场景下，竞品与 076 相比，绝对接收能力和抗干扰能力各如何？"

### 1.4 结果能说明什么、不能说明什么

**能说明**：
- "天线 + M21 整机"在该干扰场景下的**系统级**表现差异（C/N0 保持、丢星、观测量质量退化）。
- 同一干扰环境下两根天线的**相对**优劣（因为两台接收机同步、同场景采集，环境变量基本抵消）。

**不能说明**：
- **不能说是天线单独的功劳**。M21 内置抗干扰算法在 UG016 协议里没有关闭开关，
  所有结论都是"天线 + M21 算法"打包后的系统级结论。报告开头和结尾都明确写了这句话。
- **不能当作认证结论**。干扰源是消费品（手机、路由器、对讲机），发射功率随业务动态变化；
  15 cm 属于近场，场强空间梯度大，两根天线受到的干扰强度并不严格相等。
  这是工程筛查/选型对比，不是 ETSI 认证测试。
- **单次测试不能排除几何误差**。干扰源偏向哪根天线一侧，哪根就吃亏；严谨做法应交换天线位置复测。

---

## 二、怎么读一份对比报告（逐章节详解）

对比报告共 19 个章节。下面逐章说明：这章回答什么问题、每列是什么意思、数字从哪个数据源来、
怎么看正常/异常。示例数字均来自 0728-076-560 报告。

> **关于颜色**：对比报告的表格**没有**红绿灯式的单元格着色（`.good`/`.bad` 样式虽然在页面里定义了，
> 但对比报告的表格并未使用；仅单天线报告在片段缺失时会用红色文字提示）。
> 所以判断好坏要靠你自己按本指南第三章的标准读数，不要指望颜色。

### 第 1 章 数据时间范围与片段追溯

- **回答的问题**：这份报告分析的是哪段原始数据？9 个片段是怎么切的？
- **怎么读**：给出 GPS 周和周内秒范围（例：GPS 周 2429，181726.200 s ~ 182147.000 s，共 420.8 s），
  以及每段的定义时长、起止时间、**分析窗口起止**（中间 10 秒）。
- **意义**：这是可复核性保证。任何后续章节的"某片段"数字，都能对应回原始数据的具体 10 秒。
  若"窗口时长"列带 `*`，说明该段被文件截断，数据不足定义时长。

### 第 2 章 整体指标对比

- **回答的问题**：不看分场景，整个文件下来两根天线的总体面貌如何？
- **逐列说明**（除特殊说明外均来自 COM3 TRACKSTAT，统计范围为全文件、所有通道记录）：

| 列 | 含义 | 单位/口径 |
|---|---|---|
| C/N0 中位数 / 均值 | 全部通道记录的载噪比中位数/均值 | dB-Hz；注意是"所有卫星×所有频点×所有历元"混在一起 |
| PLL 锁定比 | 通道状态为 PLL_LOCK 的比例 | 越高越好，本组数据都是 100.0% |
| locktime<0.5s | locktime（连续锁定时长）不足 0.5 秒的记录占比 | 反映"刚失锁重捕"的频繁程度，越低越好；例：076/560 都是 9.8% |
| reject 比例 | 被接收机标记为非 GOOD（伪距残差被拒）的比例 | 越低越好，本组为 0.0% |
| 平均跟踪星数 | 每个历元平均跟踪几颗卫星 | TRACKSTAT 逐历元去重统计 |
| 平均参与解算星数 | 每个历元平均有几颗进入定位解算 | 来自 BESTPOS 的 `#satsln`；例：076 = 30.6，560 = 26.3 |
| 伪距 RMS 中位数 | 定位伪距残差 RMS | 来自 GPGST，单位 m；例：都是 0.030 |

- **异常信号**：PLL 锁定比明显低于 100%、reject 比例非零、伪距 RMS 显著增大（如 >0.1 m）。
- **注意**：这一章是"混合口径"——它包含了干扰段和无干扰段、所有卫星所有频点，
  且跟踪星数多的天线会拉低/抬高中位数（卫星构成不同）。**总体接近不代表分场景接近**，
  例如此处 076 与 560 的 C/N0 中位数只差 0.5 dB（42.75 vs 42.25），但后面会看到两者在强干扰下天壤之别。

### 第 3 章 各系统 C/N0 中位数对比

- **回答的问题**：分卫星系统（BDS/GAL/GLO/GPS/QZS）看，两根天线各自收哪个系统更强？
- **列**：天线、系统、C/N0 中位数、C/N0 均值、样本数（通道记录数，不是历元数）。
- **怎么读**：这是**全文件混合**（含干扰段）的分系统画像，用来发现"某天线在某系统上系统性偏强/偏弱"。
  例：560 在 GAL/GLO/GPS/QZS 上中位数都高于 076（如 GAL 46.00 vs 44.25），唯独 BDS 低于 076（40.50 vs 42.50）。
  这个"BDS 反转"现象在后面第 5 章还会遇到，是理解这组对比的关键细节。
- **样本数差异**：560 各系统样本数普遍比 076 少（如 BDS 45783 vs 54494），
  因为 560 在干扰段丢星丢通道多，记录自然少——样本数本身就是抗干扰差异的旁证。

### 第 4 章 各片段 C/N0 中位数对比

- **回答的问题**：9 个片段逐个看，两根天线在每个 10 秒窗口里的总体 C/N0 水平。
- **列**：天线、片段、C/N0 中位数 / p25 / p75（窗口内全部通道记录的分位数）、PLL 锁定比、
  跟踪星数、参与解算星数（BESTPOS）、伪距 RMS（GST）。
- **怎么读**：这是**最直观的一章**，配合柱状图看两根柱子的高差随场景变化。例：

| 片段 | 076 C/N0 中位数 | 560 C/N0 中位数 | 解读 |
|---|---|---|---|
| 无干扰 #1 | 43.75 | 42.25 | 平时两者接近 |
| 409MHz 干扰 | **37.00** | **29.25** | 076 掉 6.75 dB，560 掉 13 dB，差距拉开 |
| 392MHz 干扰 | **33.00** | **31.38** | 560 此时只跟踪到 1 颗星（见下） |
| 4G/5G、Wi-Fi 干扰 | ≈42.75 / 43.00 | ≈42.25 | 这两种干扰对两者都几乎无影响 |

- **异常信号**：干扰段的"跟踪星数"骤降是最硬的异常——560 在 392MHz 段跟踪星数 = 1、参与解算星数 = 0.0，
  意味着**整机已经失定位**；此时它的 C/N0 中位数 31.38 只是侥幸残存的一颗星的值，不代表整体水平，
  切勿拿它和 076 的 33.00 直接比"只差 1.6 dB"。
- **口径提醒**：本章和下一章可能给出**方向相反**的结论，原因见第 5 章末尾的"口径提醒"。

### 第 5 章 同片段同系统 076 − 竞品 的 C/N0 中位数差值

- **回答的问题**：剔除"跟踪的卫星不同"这个干扰因素后，同一时段、同一系统、**共同卫星**上谁更高？
- **算法**：对每个片段、每个系统，取两根天线都跟踪到的卫星，逐星算"076 窗口中位数 − 竞品窗口中位数"，
  再取这些差值的中位数/p25/p75。"共同星数"列就是参与配对的卫星数。
- **符号约定（重要）**：**正值 = 076 更高，负值 = 竞品更高**。报告内文也写了"±0.5 dB 内视为持平"。
- **怎么读**（0728-076-560 实例）：
  - 无干扰各段：BDS 行是正数（+1.88 ~ +2.44，076 的 BDS 更强），
    而 GAL/GLO/GPS/QZS 行全是负数（约 −0.75 ~ −3.5，560 更强）。
    即"560 基线更强"主要体现在非 BDS 系统上。
  - 409MHz 干扰：所有系统大幅转正（BDS +9.00、GAL +11.25、GPS +15.19）——干扰下 076 全面碾压。
  - 392MHz 干扰：只剩 BDS 3 颗、GLO 1 颗共同卫星（560 几乎丢光），共同星数极少时这行的代表性有限。
- **口径提醒（pooled vs paired）**：第 2/4 章是"pooled"口径——把窗口内所有通道记录混在一起取中位数，
  卫星构成会影响结果（BDS 星数最多，话语权最大）；本章和第 7 章是"paired"口径——逐星配对后再取中位数，
  每颗卫星话语权相等。两者可以方向相反：本组数据中 pooled 口径下 076 基线略高（第 19 章结论里的
  "基线差值 +0.75 dB"就是这么算的），而 paired 口径下 560 基线高约 1 dB（见第 7 章无干扰段全为负值）。
  **比较两根天线的固有能力时，paired 口径（本章和第 7 章）更公平**，因为它不受"谁跟的星多"影响。

### 第 6 章 逐星 C/N0 相关性分析

- **回答的问题**：两根天线对同一电磁环境的**变化趋势**是否一致？（注意：不是比谁好！）
- **算法**：对两颗天线都观测到的每颗卫星，按同一 GPS 周内秒配对两条 C/N0 时间序列，
  算 Pearson 相关系数 r。配对样本数 < 30 的卫星被剔除。
- **6.1 总体摘要各列**：
  - **总体样本 r**：把所有卫星的配对样本合并后算的一个 r。它会被"不同卫星 C/N0 基线不同"放大，
    数值偏高、容易误读。例：0.689 看起来挺高。
  - **Fisher-z 平均 r**：先对每颗星的 r 做 Fisher z 变换再平均、再变回来，是**跨卫星平均相关性**的正确算法，
    是本章最该看的数。例：0.300（弱相关）。
  - r 中位数/均值/p25/p75：逐星 r 的分布。例：中位数 0.296。
  - 强相关数/中等相关数/弱相关数：逐星 r 按 |r| ≥ 0.7 / 0.4–0.7 / < 0.4 分档计数。例：34 颗星里仅 1 颗强相关、25 颗弱相关。
- **6.2 按系统摘要**：同上按系统分组。
- **6.4 逐星明细**：每颗星的 r 和强度分档。
- **怎么读（关键）**：**r 高只说明两根天线的 C/N0 同涨同跌，不说明谁接收得好。**
  两根天线相距 30 cm 看同一片天空，变化趋势天然相关；r 低通常意味着通道级噪声/丢星打断了同步变化
  （本组 Fisher-z 平均 r 只有 0.300，很大原因是 560 在干扰段大量丢星、序列残缺）。
  这个章节是"数据质量/一致性检查"，不是"优劣评判"。
- **口径提醒**：TRACKSTAT 里每颗卫星按频点有多条通道记录，配对按 sow 内连接，
  因此"配对样本数"（例：BDS35 = 15520）远大于实际历元数（全文件 5 Hz × 420 s ≈ 2100），
  是"通道对"的数量级，只用于判断样本是否充分，不要按字面当历元数解读。

### 第 7 章 显著性检验（Wilcoxon 符号秩检验）

- **回答的问题**：第 5 章看到的差值，是稳定的系统性差异，还是随机波动？
- **算法**：每个片段内，取共同卫星的"076 窗口中位数 − 竞品窗口中位数"配对差值
  （要求每颗星在两根天线的窗口内都 ≥5 个历元，否则该星剔除），做 Wilcoxon 符号秩检验。
- **逐列**：
  - **配对卫星数**：通过样本量过滤、实际参与检验的卫星数。
  - **差值中位数**：符号同第 5 章——**正 = 076 高，负 = 竞品高**。
  - **Z 值 / p 值**：p < 0.05 判为"显著"。
  - **显著性**：显著 / 不显著。
- **怎么读**（0728-076-560 全表，这是本章最有教学价值的一份）：

| 片段 | 配对卫星数 | 差值中位数 (dB) | p 值 | 显著性 | 解读 |
|---|---|---|---|---|---|
| 无干扰 #1 | 33 | −1.25 | 0.1744 | 不显著 | 560 高约 1.3 dB，但未达显著 |
| 409MHz 干扰 | 22 | **+11.06** | 0.0000 | **显著** | 076 高出 11 dB，铁证 |
| 无干扰 #2 | 33 | −1.62 | 0.1445 | 不显著 | 同上 |
| 392MHz 干扰 | **4** | +5.25 | — | 不显著 | 只剩 4 颗共同卫星，**样本不足无法检验** |
| 无干扰 #3 | 34 | −0.75 | 0.1936 | 不显著 | |
| 4G/5G 干扰 | 34 | −1.88 | 0.0045 | 显著 | 560 显著高 1.9 dB |
| 无干扰 #4 | 34 | −0.62 | 0.2241 | 不显著 | |
| Wi-Fi 干扰 | 34 | −1.75 | 0.0299 | 显著 | 560 显著高 1.8 dB |
| 无干扰 #5 | 33 | −1.25 | 0.1417 | 不显著 | |

- **两个容易误读的点**：
  1. **"不显著" ≠ "没差别"**。392MHz 段差值中位数高达 +5.25 dB，却因只剩 4 颗配对卫星
     （< 5 的最小检验样本量）直接不给出 Z/p（显示"—"）。560 都把星丢光了，才导致"没法检验"——
     这本身就是 560 惨败的证据，而不是"两者打平"。
  2. **显著的方向要看符号**。本组里 560 在 4G/5G、Wi-Fi 段"显著更高"——别只看到"显著"就以为 076 赢。
- **这一章是横向对比（076 vs 竞品同时段绝对水平）最严谨的证据。**

### 第 8 章 卫星丢失情况（干扰段相对前一无干扰段）

- **回答的问题**：干扰一来，谁把原来跟踪的卫星弄丢了？
- **判定规则（数据源：TRACKSTAT）**：某卫星在前一个无干扰段的窗口里出现过，
  且在干扰段窗口内**连续丢失 ≥3 秒**（数据约 5 Hz，折算约 15 个历元；实际按数据采样间隔自动折算），
  判为丢失。整个窗口都没出现的，丢失时长记为整窗 10 秒。
- **列**：干扰片段、076 丢失数、076 丢失卫星名单、竞品丢失数、竞品丢失卫星名单。
- **怎么读**（这是 560 报告里最震撼的一张表）：

| 干扰片段 | 076 丢失数 | 560 丢失数 |
|---|---|---|
| 409MHz | 2（GPS30, QZS385） | **14** |
| 392MHz | 3 | **33（几乎全部）** |
| 4G/5G | 0 | 0 |
| Wi-Fi | 0 | 0 |

- **为什么用"连续 ≥3 秒"**：干扰下接收机常出现瞬态断链又立刻重捕，只看"是否出现过"会严重高估保持能力；
  连续 3 秒才认为这颗星真的帮不上忙了。
- **n_gained（新增卫星）**：干扰段新出现的卫星不计入丢失统计，报告中未单列。

### 第 9 章 干扰退化对比（双天线同屏）

- **回答的问题**：同一个干扰场景下，两根天线各自"干扰段 vs 自己前一基线"衰减了多少 dB、丢了几颗星？——**纵向对比的核心表**。
- **算法**：对每颗在基线和干扰段都有 ≥5 个历元的共同卫星，算"干扰段窗口中位数 − 基线窗口中位数"，
  再取所有卫星差值的中位数，即 ΔC/N0；丢失卫星数 = 连续丢失 ≥3 秒的卫星数。
- **表格形式**：**两根天线在同一行**，每个干扰场景一行，末尾直接给出对比结论。
  报告**不再做"通过/未通过"判定**——早期版本借用过 ETSI EN 303 413 的 1 dB 参考线，
  但实测中该门限经常超限、两根天线"通过数"常常相同，掩盖了真实的量级差异，意义不大，已移除。
- **怎么读**（0728-076-560 全表）：

| 干扰片段 | 076 ΔC/N0 中位数 | 076 丢失卫星数 | 560 ΔC/N0 中位数 | 560 丢失卫星数 | 对比结论 |
|---|---|---|---|---|---|
| 409MHz 干扰 | **−4.88** | 2 | **−13.69** | 14 | 076 衰减更小（少 8.81 dB） |
| 392MHz 干扰 | **−9.00** | 3 | −11.31 | 33 | 076 衰减更小（少 2.31 dB） |
| 4G/5G 干扰 | −0.81 | 0 | 0.00 | 0 | 560 衰减更小（少 0.81 dB） |
| Wi-Fi 干扰 | −0.81 | 0 | −0.25 | 0 | 560 衰减更小（少 0.56 dB） |

- **读法**：同一行里直接比两个 ΔC/N0——谁的更接近 0，谁在这个干扰场景下抗干扰更强；
  丢失卫星数是辅助佐证（392MHz 一行：衰减差只有 2.31 dB，但 560 丢了 33 颗星，远比数字本身严重）。
- **"对比结论"列的口径**：ΔC/N0 相差 < 0.5 dB 判"两者相当"，否则判衰减更小的一方并给出差值。
  4G/5G、Wi-Fi 两行所有天线衰减都 < 1 dB，说明这两种干扰对 GNSS 频段基本没影响，
  该结论列反映的是噪声级波动，不必当真。

### 第 10 章 RANGECMP 各片段观测量质量对比

- **数据源切换**：从这一章起都是 **COM4 RANGECMP（ID 140）**，不再是 TRACKSTAT。
- **回答的问题**：干扰下伪距/载波相位的**观测噪声**变差了多少？（C/N0 是信号强度，StdDev 是测量质量，两回事。）
- **逐列**：
  - **StdDev-PSR 中位数/均值**：伪距标准差，单位 m，**越低越好**。OEM7 是量化查表值，
    本组常见档位 0.169 / 0.253 / 0.380。无干扰时两根天线都是 0.253。
  - **StdDev-ADR 中位数**：载波相位标准差，单位 cycles（周），**越低越好**。
    由 4 bit 编码换算（(code+1)/512），**0.0020 是最小档（仪表底数）**，0.0039 是第二档。
    所以看到 0.0020 → 0.0039 的跳变，意味着相位噪声翻倍，不要嫌"才 0.002"不起眼。
  - **locktime 中位数（秒）**：窗口内通道连续锁定时长的中位数。**这是本章隐藏的重要指标**——
    无干扰时是几百秒（如 261.1 s），干扰下暴跌到个位数甚至 0.2 s，
    说明通道在反复失锁重捕。例：560 在 409MHz 段 locktime 中位数 0.2 s，392MHz 段 0.0 s。
  - **Doppler 标准差**：窗口内多普勒观测的离散度，辅助参考，无固定好坏阈值，看相对变化。
  - **C/N0 中位数**：RANGECMP 口径的 C/N0，和第 4 章 TRACKSTAT 口径趋势一致，可互相印证。
  - **样本数**：窗口内通道记录数，骤降 = 丢通道（560 在 392MHz 段只剩 34 条，076 有 2374 条）。
- **典型范围**：无干扰 StdDev-PSR ≈ 0.17–0.25 m、StdDev-ADR = 0.0020、locktime 数十到数百秒；
  强干扰下 StdDev-PSR 升到 0.38 档、StdDev-ADR 升档、locktime 掉到秒级即为明显退化。

### 第 11 章 RANGECMP 干扰段伪距标准差退化对比

- **回答的问题**：纵向看，干扰段的 StdDev-PSR 比自己基线增大了多少？
- **算法**：共同卫星逐星算"干扰段 StdDev-PSR 中位数 − 基线 StdDev-PSR 中位数"，再取中位数/p25/p75。
  **ΔStdDev-PSR > 0 = 变差**，"退化卫星数"= Δ > 0 的卫星数。
- **怎么读**（例）：409MHz 下 076 的 Δ 中位数 0.000（仅 2 颗星退化），
  560 的 Δ 中位数 **+0.127 m**、22 颗共同卫星里 16 颗退化——观测量质量全面恶化。
- **注意"共同卫星数"的幸存者偏差**：560 在 392MHz 段只剩 4 颗共同卫星，
  Δ 中位数 +0.064 看起来不严重——但那是因为退化最狠的星已经丢光、根本没进入统计。
  **共同卫星数骤减本身就是结论**，切勿只看 Δ 数值。

### 第 12 章 RANGECMP 按系统伪距标准差退化对比

- 与第 11 章同一算法，按 GNSS 系统（BDS/GAL/...）分组展开，多了"改善卫星数"（Δ < 0）。
- **用途**：定位"哪个系统被打得最狠"。例：560 在 409MHz 段 GAL 5 颗全部退化、
  Δ 中位数 +0.211 m（E1 频点受 409MHz 影响大，和第 16/17 章频点分析互证）。

### 第 13 章 RANGECMP 载波相位标准差（StdDev-ADR）对比

- 按"片段 × 系统"列出 StdDev-ADR 中位数/均值/p75。**低好**，底数 0.0020。
- **怎么读**：无干扰时全表都是 0.0020（底数）；干扰段看哪些系统升档。
  例：409MHz 下 560 的 GAL/GPS/QZS 全部升到 0.0039，076 只有 BDS 的 p75 升到 0.0039。
  由于只有 4 bit 量化、档位很粗，**升一档就是翻倍**，别按绝对值小就忽略。
- **对 RTK 用户的意义**：StdDev-ADR 直接反映载波相位质量，相位噪声大 → 固定解难维持。

### 第 14 章 联合失锁分析对比（TRACKSTAT + RANGECMP）

- **回答的问题**：用两个独立数据源交叉验证"丢星"判定，排除单一数据源的假象。
- **判定规则**：基线卫星集合取 **TRACKSTAT 与 RANGECMP 的并集**（所以"共同卫星数"比第 8 章多，
  例：37 vs 33）；对每颗星分别判定：TRACKSTAT 里连续缺失 ≥3 秒（track_lost）、
  RANGECMP 里连续缺失 ≥3 秒（rc_lost）、两者都丢（both_lost）。
- **列**：天线、干扰片段、共同卫星数、TRACKSTAT 丢失数、RANGECMP 丢失数、两源均丢失数。
- **怎么读**（例）：409MHz 下 560 三列是 16/16/15——两个源几乎一致地判定 15 颗星真丢，高置信；
  076 是 5/5/3。392MHz 下 560 是 35/35/35——36 颗基线卫星里 35 颗两源均丢，判死刑级别。
- **和第 8 章数字不同是正常的**：第 8 章只用 TRACKSTAT、基线集合也只取 TRACKSTAT 的星，
  本章基线是两源并集且分别计数（560 在 409MHz：第 8 章 14 颗 vs 本章 track_lost 16 颗）。

### 第 15 章 两源均丢失卫星名单对比

- 第 14 章 both_lost 的逐星明细：卫星、系统、TRACKSTAT 最大丢失时长、RANGECMP 最大丢失时长（秒）。
- **怎么读**：丢失时长 = 10.0 表示**整个 10 秒分析窗口内一秒都没跟到**（彻底丢失）；
  例：076 在 409MHz 下 GPS30 丢了 4.0 秒（中途恢复），而 560 一长串 10.0（全窗丢失）。
  看时长可以区分"擦边过 3 秒阈值的短暂失锁"和"整段消失"。

### 第 16 章 RANGECMP 按频点观测量质量对比

- **回答的问题**：干扰到底打在哪些频段上？
- **频点拆分**：RANGECMP 通道状态里的信号类型字段映射为 L1/L2/L5/E1/E5a/E5b/B1/B2/B3 等频段，
  按"片段 × 频段"给出 StdDev-PSR、StdDev-ADR、C/N0、locktime 中位数。
- **背景知识**（报告原文已注明）：409 MHz 的 3 次谐波 1227 MHz 距 L2 约 0.60 MHz；
  392 MHz 的 3 次谐波 1176 MHz 距 L5/E5a 约 0.45 MHz。但实际数据显示受影响频段更宽，
  说明存在宽带阻塞 + 谐波耦合的混合机制。
- **怎么读**（例）：409MHz 下 076 的 B3/E5a C/N0 从 42.5/44 掉到 29/30，而 L1/B1 只掉 1 dB——
  频点选择性清晰可见。某频段"样本数"锐减或整行消失 = 该频段被打没（560 在 392MHz 只剩 B1、L1 两行）。

### 第 17 章 RANGECMP 按频点伪距标准差退化对比

- 第 16 章的纵向版：每频段的"基线 vs 干扰段"StdDev-PSR 和 C/N0 变化量。
- **列**：基线/干扰 StdDev-PSR、ΔStdDev-PSR（正 = 变差）、基线/干扰 C/N0、ΔC/N0（负 = 衰减）。
- **怎么读**（例，409MHz）：076 的 E5a ΔC/N0 = −14.0 dB 且 ΔStdDev-PSR = +0.127，而 L1/B1 只有 −1.0/0.000；
  560 的 E1、L1 ΔC/N0 都是 **−20.0 dB**——这张表是分析干扰机理（谐波 vs 宽带）的主要依据。
- 同样需要警惕幸存者偏差：频段整行缺失说明该频段在干扰段样本不足（< 3 条记录），根本没列入。

### 第 18 章 StdDev-ADR 与 C/N0 联合散点对比

- 2×2 散点图（4 个干扰段各一幅）：横轴 C/N0、纵轴 StdDev-ADR，颜色 = 频点，形状 = 天线（圆 = 076，方 = 竞品）。
- **怎么读**：正常状态点应堆在"右下"（高 C/N0、低 StdDev-ADR）。若干扰下某天线的点群向"左上"移动
  （C/N0 掉、ADR 升），说明该频段观测质量恶化。用来直观定位"哪个频点在哪类干扰下被打"。

### 第 19 章 综合结论

报告自动生成的 9 条结论，是前面各章的汇总，**读之前请先理解它的生成规则，否则会误读**：

1. **整体水平**：套话，恒为"两者接近"（基于第 2 章混合口径），不用细看。
2. **相关性**：引用第 6 章 Fisher-z 平均 r 和逐星中位数。记住：这是趋势一致性，不是优劣。
3. **显著性检验**：数第 7 章里"显著且差值为正/负/不显著"的片段数。
   例：560 报告为"1 个片段 076 显著更高、2 个片段 560 显著更高、6 个无显著差异"——
   单看这条会以为 560 占优，但显著的 2 段（4G/5G、Wi-Fi）差值只有 −1.9/−1.8 dB，
   而 076 显著赢的 409MHz 段差值 +11.06 dB。**数片段个数掩盖了量级**。
4. **抗干扰性**：干扰段平均 ΔC/N0（076 平均 −3.88 dB vs 560 平均 −6.31 dB）——越接近 0 抗干扰越强。
5. **卫星保持**：第 8 章丢失数累计（076 = 5 颗次，560 = 47 颗次）——**这条是硬指标，重点看**。
6. **分场景退化**：逐干扰场景比较两根天线的 ΔC/N0 中位数，差 > 0.5 dB 才分胜负，否则"基本持平"。
7. **绝对接收能力**：基线各段的"片段 pooled C/N0 中位数之差（076 − 竞品）"的中位数，> +0.5 dB 判 076 基线高，
   < −0.5 dB 判竞品高；干扰段同理。注意这是 pooled 口径（见第 5 章口径提醒）。
8. **相对抗干扰能力**：同第 6 条的文字罗列。
9. **总体性能判定**：综合第 7 条给出四种判定之一（076 强 / 竞品强 / 各有优势 / 接近）。

- **结论的正确用法**：先自己看第 4、7、8、9 章的原始数字形成判断，再用第 19 章核对。
  结论里"片段数计数"类表述（第 3、4 条）权重最低，"衰减量、丢星数"类表述（第 5、6 条）权重最高。
- **已知文字瑕疵**：当基线差值判定为持平时，句子会读作"0.00 dB（两者持平 基线更高）"
  （0731-076-by403 报告第 7 条），是模板拼接问题，语义以"两者持平"为准。

### 单天线报告与 CSV（顺带说明）

每组目录下还有 2 份单天线报告（`antenna_{测试名}_{天线名}_report.html`），
内容是该天线自己的逐星统计、时间序列图、分片段指标，用于下钻细节；
CSV 文件是报告所有表格的原始数据（如 `segment_wilcoxon_*.csv`、`interference_assessment_*.csv`、
`satellite_loss_*.csv`），需要二次计算或画图时直接用 CSV，不必从 HTML 抄数。

---

## 三、指标速查表（高好还是低好）

| 指标 | 出现章节 | 数据源 | 好 = | 怎么看 |
|---|---|---|---|---|
| C/N0 | 2/3/4/10/16 | TRACKSTAT / RANGECMP | **高** | dB-Hz；无干扰约 40–46，干扰段掉得越少越好 |
| ΔC/N0（衰减量） | 9/17 | TRACKSTAT / RANGECMP | **越接近 0 越好**（负得越少越好） | −1 dB 内优秀；−5 dB 明显退化；−10 dB 以下严重 |
| 丢失卫星数 n_lost | 8/9/14/15 | TRACKSTAT（+RANGECMP 联合） | **低** | 0 最好；两位数 = 被打穿；同时看丢失时长（10.0 s = 全窗丢失） |
| StdDev-PSR | 10/11/12/16/17 | RANGECMP | **低** | 单位 m，量化档位 0.169/0.253/0.380…；Δ > 0 = 退化 |
| StdDev-ADR | 10/13/16/18 | RANGECMP | **低** | 单位 cycles；0.0020 是底数，升一档即翻倍 |
| locktime 中位数 | 10/16 | RANGECMP | **高** | 无干扰数十~数百秒；掉到秒级 = 反复失锁重捕 |
| locktime<0.5s 比例 | 2 | TRACKSTAT | **低** | 刚重捕的通道占比 |
| PLL 锁定比 | 2/4 | TRACKSTAT | **高** | 本组恒 100%，区分度低 |
| reject 比例 | 2 | TRACKSTAT | **低** | 理想为 0% |
| 伪距 RMS | 2/4 | GPGST | **低** | 正常 ≈ 0.03 m；> 0.1 m 异常 |
| 跟踪星数 / 参与解算星数 | 2/4 | TRACKSTAT / BESTPOS | **高** | 干扰段骤降 = 失锁/失定位；参与解算 0 = 已失定位 |
| Pearson r / Fisher-z 平均 r | 6 | TRACKSTAT | 高 = 两天线**趋势一致** | **不代表谁好**；r ≥ 0.7 强、0.4–0.7 中等、< 0.4 弱 |
| wilcoxon_p | 7 | TRACKSTAT | < 0.05 = 差异显著 | 方向看 median_diff 符号（正 = 076 高）；n < 5 无法检验 ≠ 无差异 |
| 差值中位数（第 5/7 章） | 5/7 | TRACKSTAT | 正 = 076 高，负 = 竞品高 | ±0.5 dB 内视为持平 |
| ΔC/N0 中位数（第 9 章） | 9 | TRACKSTAT | 越接近 0 越好 | 干扰段 vs 自己前一基线的衰减量；同一行直接比两根天线 |

---

## 四、对比逻辑详解（最容易混淆的一章）

### 4.1 两个对比维度

这份报告里其实同时存在两种对比，回答的是两个不同问题：

**① 纵向对比（本体 vs 本体）——衡量"抗干扰能力"**

同一根天线，干扰段和它自己的前一无干扰基线比，得到衰减量 ΔC/N0、丢星数、StdDev 退化。
对应章节：**第 8、9、11、12、17 章**。
问题："干扰来了，这根天线比自己平时差了多少？"——掉得越少，抗干扰越强。
它**排除了**两根天线增益不同的影响（各自跟自己比），所以是抗干扰的公平度量。

**② 横向对比（076 vs 竞品）——衡量"绝对接收能力"**

同一时间段内，两根天线的绝对 C/N0 直接比（第 5 章差值、第 7 章 Wilcoxon）。
对应章节：**第 4、5、7 章**（第 2、3 章是辅助画像）。
问题："同一时刻，谁收到的信号更强？"——反映天线增益、方向图、低噪放等固有能力。
它**包含了**两根天线的固有差异，所以不能单独用来证明"谁抗干扰好"。

### 4.2 为什么两个维度都必须看：560 反差案例

0728-076-560 这组是教科书级案例，两个维度给出**相反**的答案：

- **横向（绝对能力）**：无干扰各段的配对差值全为负（−0.62 ~ −1.62 dB），
  即 **560 的无干扰基线 C/N0 反而比 076 高约 1 dB**；4G/5G、Wi-Fi 段 560 甚至"显著更高"。
  单看这个维度，会得出"560 比 076 好"的结论。
- **纵向（抗干扰）**：409MHz 下 076 衰减 −4.88 dB、丢 2 星；560 衰减 **−13.69 dB、丢 14 星**。
  392MHz 下 076 衰减 −9.00 dB、丢 3 星；560 衰减 **−11.31 dB、丢 33 星（几乎全部），
  跟踪星数掉到 1、参与解算 0、locktime 中位数 0 秒**——整机失定位。

**解读**：560 是一只"晴天天线"——无干扰时增益略高，但前端在强带外干扰下迅速饱和/阻塞；
076 平时增益略低，但干扰下保持了可用性。对抗干扰选型而言，纵向维度（第 8、9 章）是一票否决项：
**无干扰时高 1 dB 没有意义，409MHz 下 076 实际可用 C/N0（37.0）反而高出 560（29.25）近 8 dB。**
这就是第 19 章最终判"076 整体性能更强：基线更高且干扰段绝对水平仍保持领先"的依据
（注：该判定用的是 pooled 口径基线差 +0.75 dB；paired 口径下基线其实是 560 略高，
但"干扰段绝对水平反超"这一半结论在两种口径下都成立且量级巨大，总结论方向不变）。

反向案例也存在：0731-076-speed 报告中，speed 纵向退化比 076 还小（409MHz −2.25 vs −3.25 dB），
但横向基线低 1.5 dB 且 9 个片段里 8 个 076 显著更高——结论仍是 076 整体更强。
**纵向赢 + 横向输 = 各有取舍；纵向输 + 横向输（如 560 的强干扰场景）= 明确出局。**

### 4.3 一张决策图

```
看第 9 章 ΔC/N0 与第 8 章丢星数（纵向）
        │
        ├─ 竞品衰减明显更大/丢星明显更多 → 竞品抗干扰差（强干扰场景一票否决）
        │
        └─ 纵向相当 → 看第 7 章 Wilcoxon（横向）
                        │
                        ├─ 多数片段竞品显著更高 → 竞品绝对能力更强
                        ├─ 多数片段 076 显著更高 → 076 更强
                        └─ 多数不显著 → 两者相当，看第 5 章分系统差值找细节
```

---

## 五、4 份报告之间怎么对比

### 5.1 为什么都锚定 076

4 组测试里 076 永远是 first_ant（报告的"前者"），所有差值、Wilcoxon median_diff 的符号约定都是
**"076 − 竞品"，正 = 076 高**。这个统一锚点的意义：竞品的绝对 C/N0 受当天环境（卫星几何、
大气、干扰功率）影响，不能直接跨天比；但"竞品相对 076 的差值"把当天环境约掉了，可以跨组比较。

### 5.2 跨组不能比绝对值

076 自己的基线在不同测试日就有明显漂移——整体 C/N0 中位数：

| 测试组 | 076 整体 C/N0 中位数 |
|---|---|
| 0728-076-560 | 42.75 |
| 0728-076-617 | 41.75 |
| 0731-076-by403 | 44.75 |
| 0731-076-speed | 44.75 |

0728 ≈ 41–43 dB，0731 ≈ 44–45 dB，相差 2–3 dB（卫星几何、天气、电缆状态等都会变）。
**所以"560 基线 42.25 vs by403 基线 44.75，by403 好 2.5 dB"这种说法是错的**——它们不在同一天、
不共享环境。同样，076 在 409MHz 下的衰减各组也不同（−4.88 / −2.56 / −3.56 / −3.25 dB），
因为干扰源功率每次都在变，**干扰强度本身不可跨组复现**。

### 5.3 正确的跨组比法：比"竞品相对 076 的差值"

对每个干扰场景，算"竞品 ΔC/N0 − 076 ΔC/N0"（竞品衰减 − 076 衰减，负 = 竞品抗干扰更差）：

| 竞品 | 409MHz（竞品Δ − 076Δ） | 392MHz | 丢星对比（409/392，076 vs 竞品） |
|---|---|---|---|
| BT-T560 | −13.69 −(−4.88) = **−8.81 dB** | −11.31 −(−9.00) = −2.31 dB | 2/3 vs 14/33 |
| BT-T617 | −0.88 −(−2.56) = **+1.68 dB** | −6.88 −(−5.75) = −1.13 dB | 0/3 vs 2/4 |
| by403 | −7.31 −(−3.56) = **−3.75 dB** | −11.38 −(−8.94) = −2.44 dB | 0/0 vs 1/3 |
| speed | −2.25 −(−3.25) = **+1.00 dB** | −6.25 −(−7.38) = +1.13 dB | 1/1 vs 0/2 |

这样排出来的抗干扰梯队才是可比的：409MHz 场景 560（−8.81）和 by403（−3.75）明显弱于 076，
617 和 speed 略强；392MHz 场景 speed 略强，560/by403/617 均弱于 076。
横向绝对能力同理：用各组 Wilcoxon 无干扰段的差值中位数比（560 ≈ −1 dB 即强于 076，
speed ≈ +1.5 dB 即弱于 076），而不是比各自的绝对 C/N0。

### 5.4 跨组对比的注意事项

- 干扰功率不稳定：竞品 A 在 409MHz 衰减大，可能部分因为那次对讲机离它更近/功率更大。
  差距 < 1 dB 的跨组比较没有实际意义。
- 丢星数比 ΔC/N0 更稳健（对功率波动的敏感度低），跨组对比时优先看丢星数和 locktime。
- 两组 0728 共享同一天的 076 数据环境，两组 0731 同理；同日的两组之间可比性略好于跨日。

---

## 六、常见误读与局限

1. **相关性高 ≠ 天线好**。第 6 章的 r 只衡量两根天线 C/N0 变化趋势是否同步，
   30 cm 间距下天然相关；560 组 Fisher-z 平均 r 只有 0.300，反映的恰是 560 干扰段大量丢星。
2. **衰减量必须看具体数值，不要只看"谁赢谁输"的标签**。第 9 章"对比结论"列在差 < 0.5 dB 时判
   "两者相当"，但 4G/5G、Wi-Fi 场景所有天线衰减都 < 1 dB，该列只是噪声级波动；
   真正有价值的是 409/392MHz 行的 Δ 数值和丢失卫星数（076 −4.88 vs 560 −13.69，差近 9 dB）。
3. **"不显著" ≠ "没差别"**。Wilcoxon 在配对卫星 < 5 颗时不给 p 值（显示"—"）；
   560 的 392MHz 段正是因丢星太多只剩 4 颗配对卫星才"不显著"，实际是惨败。
4. **数"显著片段个数"会掩盖量级**。560 组结论第 3 条"076 赢 1 段、560 赢 2 段"，
   但 076 赢的那段差 +11 dB，560 赢的两段只差 −1.9/−1.8 dB。
5. **干扰段 C/N0 中位数有幸存者偏差**。丢星越多，剩下的越是强星，中位数越"好看"；
   560 在 392MHz 段中位数 31.38 只是残存 1 颗星的值。务必与跟踪星数/丢星数同看，
   第 11/17 章的"共同卫星数"骤减同理。
6. **pooled 与 paired 口径可能方向相反**。第 2/4 章（混合所有记录）受卫星构成影响，
   BDS 星多话语权大；第 5/7 章（逐星配对）更公平。比固有能力看后者。
7. **结论是系统级的**。M21 抗干扰算法不可关，所有结果 = 天线 + 算法的打包表现；
   换接收机结论可能不同。
8. **近场几何与功率波动**。15 cm 近场下干扰对两根天线并不严格公平；手机/路由器功率随业务波动。
   单次结果差距 < 1 dB 不足为凭，重要结论应交换位置复测。
9. **4G/5G 与 Wi-Fi 两场基本没有区分度**。4 组测试里这两种干扰对两根天线的 ΔC/N0 都在 ±1 dB 内、
   零丢星，说明该干扰强度下整机无感；真正有区分度的是 409/392 MHz 两场，解读时把注意力放在这两场。
10. **分析窗口只有中间 10 秒**。干扰开/关过渡期的行为（捕获时间、恢复速度）不在统计内；
    要看恢复过程需翻单天线报告的时间序列图。

---

*本指南与 `scripts/gnss_analyzer.py`（指标计算）、`scripts/gnss_reporter.py`（报告生成）、
`config/tests.yaml`（片段定义）口径一致。若后续代码修改导致报告结构变化，请同步更新本文件。*
